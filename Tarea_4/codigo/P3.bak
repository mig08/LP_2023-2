#lang scheme

(define(largo lista)
  (if (null? lista)
      0
      (+ 1 (largo(rest lista)))))

(define (chekear cantidad lista)
  (let ((x (largo (first lista))))
    (if (= x cantidad)
        true
        false)))

(define (armar_lista stock)
  (if (null? stock)
      '()
      (let ((listita (first stock)))
        (letrec ((cant (first listita)))
          (
          

          
          
  
      
          





(armar_lista '((3 (tomate tomate tomate)) (2 (ajo))))
